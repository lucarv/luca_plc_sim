[
    {
        "nodeId": "s=counter",
        "browseName": "Product Counter",
        "dataType": "Int32",
        "func": "let i = Math.random(); let c = 0; if (Math.round(i * 1000) %17 == 0) { c += 1; } c"
    },
   {
        "nodeId": "s=torque",
        "browseName": "Torque",
        "dataType": "Double",
        "func": "let i = Math.random(); if (Math.round(i * 1000) %191 == 0) { i *= 10; } i"
    }
]
